{
  "name": "live",
  "private": true,
  "scripts": {
    "dev": "netlify dev --context production",
    "dev:local": "bash dev.sh",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vitest": "^4.1.10"
  }
}
