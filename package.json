{
  "name": "live",
  "private": true,
  "scripts": {
    "dev": "netlify dev --context production",
    "dev:local": "bash dev.sh",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
